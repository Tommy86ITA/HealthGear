@{
    ViewData["Title"] = "Conferma Reset Password";
}

<h2 class="mb-4"><i class="fas fa-key"></i> Reset Password Completato</h2>

<div class="alert alert-success">
    La password per l'utente <strong>@ViewData["UserName"]</strong> è stata reimpostata correttamente.
</div>

<p><strong>Nuova Password Temporanea:</strong></p>
<div class="alert alert-warning text-monospace fs-5">
    @ViewData["NewPassword"]
</div>

<div class="mt-4">
    <a asp-action="Index" class="btn btn-primary">
        Torna alla lista utenti
    </a>
</div>