@{
    Layout = "_Layout"; // Usa il tuo layout, se necessario
    ViewData["Title"] = "File non disponibile";
}

<div class="container mt-5">
    <div class="alert alert-warning alert-dismissible fade show shadow" role="alert">
        <h4 class="alert-heading">Ops, file non disponibile!</h4>
        <p>
            Il file che stai cercando non è presente sul server. Potrebbe essere stato
            rimosso manualmente o spostato.
        </p>
        <hr />
        <p class="mb-0">
            Se pensi che sia un errore, contatta l'amministratore.
        </p>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Chiudi"></button>
    </div>

    <div class="card shadow">
        <div class="card-body text-center">
            <p class="mb-3">Puoi tornare indietro alla pagina precedente.</p>
            <a href="javascript:history.back()" class="btn btn-primary">
                <i class="fas fa-arrow-left"></i> Torna indietro
            </a>
        </div>
    </div>
</div>